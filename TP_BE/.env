MONGODB_URI=mongodb://127.0.0.1:27017/thinhphathotel

MOMO_PARTNER_CODE=MOMOBKUN20180529
MOMO_ACCESS_KEY=klm05TvNBzhg7h7j
MOMO_SECRET_KEY=at67qH6mk8w5Y1nAyMoYKMWACiEi2bsa

MOMO_CREATE_ENDPOINT=https://test-payment.momo.vn/v2/gateway/api/create
MOMO_QUERY_ENDPOINT=https://test-payment.momo.vn/v2/gateway/api/query
# Refund endpoint (sandbox default)
MOMO_REFUND_ENDPOINT=https://test-payment.momo.vn/v2/gateway/api/refund
# Toggle real refund API (if false -> admin processRefund uses mock)
MOMO_ENABLE_REFUND=true
# captureWallet = ví MoMo + QR (ổn định sandbox); payWithATM = Napas; payWithCC = thẻ QT
MOMO_REQUEST_TYPE=captureWallet
# Chỉ khi QR báo đúng nhưng cần gửi userInfo: MOMO_ATTACH_USER_INFO_CAPTURE_WALLET=true (mặc định QR không gửi)
# Verify signature for callback/ipn (recommended)
MOMO_VERIFY_SIGNATURE=true

# URL
BASE_URL=http://localhost:3000
FRONTEND_URL=http://localhost:5173

MOMO_REDIRECT_URL=http://localhost:3000/api/momo/callback
MOMO_IPN_URL=http://localhost:3000/api/momo/ipn

# Local-only (không deploy/ngrok):
# - MoMo có thể redirect callback về localhost qua trình duyệt (xác minh theo callback).
# - MoMo KHÔNG gọi IPN về localhost (server-to-server).
# Bật/tắt verify signature tuỳ môi trường:
MOMO_VERIFY_CALLBACK_SIGNATURE=false
MOMO_VERIFY_IPN_SIGNATURE=true

# SMTP (reset password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=dungntph32425@fpt.edu.vn
SMTP_PASS=hagv ahge qrzv trot
MAIL_FROM=ThinhPhat 

# Optional
JWT_SECRET=